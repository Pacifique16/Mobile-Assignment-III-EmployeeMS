<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeManagement">

        <activity
            android:name=".EmployeeListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EmployeeDetailActivity"
            android:exported="false" />

        <activity
            android:name=".EmployeeRegistrationActivity"
            android:exported="false" />

        <activity
            android:name=".DepartmentActivity"
            android:exported="false" />

        <activity
            android:name=".EmployeeInDepartmentDetailActivity"
            android:exported="false" />

        <service
            android:name=".MusicPlayerService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
